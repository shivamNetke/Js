// explain the includes starts with & end with function of a string

const sentence = 'the quick brown fox jumps over the lazy dog';
const word = 'fox';
console.log('the word "${word}" ${sentence.includes(word) ? 'is' : 'is not' } in the sentence');
